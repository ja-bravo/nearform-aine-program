# CI Secrets Checklist

Before deploying, ensure the following secrets are configured in GitHub:

- [ ] `GH_TOKEN` (Optional, if needed for private packages)
- [ ] `POSTGRES_PASSWORD` (Optional, if custom password is required)

*(No critical secrets are currently required for the base test pipeline.)*
